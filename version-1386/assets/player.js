(function(){var list=document.querySelectorAll("video[data-stream]");list.forEach(function(video){var stream=video.getAttribute("data-stream");if(!stream)return;if(video.canPlayType("application/vnd.apple.mpegurl")){video.src=stream}else if(window.Hls&&window.Hls.isSupported()){var hls=new window.Hls({enableWorker:true,lowLatencyMode:true});hls.loadSource(stream);hls.attachMedia(video);video._hls=hls}})})();